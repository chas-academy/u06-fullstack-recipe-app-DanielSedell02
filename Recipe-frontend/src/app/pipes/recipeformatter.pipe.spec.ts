import { RecipeformatterPipe } from './recipeformatter.pipe';

describe('RecipeformatterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeformatterPipe();
    expect(pipe).toBeTruthy();
  });
});
